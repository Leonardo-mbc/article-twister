<% @sim_box.each do |id, value| %>
    <% if id == 'user' %>
        plotter.mount('x', 'user', <%= value %>);
    <% else %>
        plotter.mount('x', <%= id %>, <%= value %>);
    <% end %>
<% end %>

plotter.plot();
