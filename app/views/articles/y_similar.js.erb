<% @sim_box.each do |id, value| %>
    <% if id == 'user' %>
        plotter.mount('y', 'user', <%= value %>);
    <% else %>
        plotter.mount('y', <%= id %>, <%= value %>);
    <% end %>
<% end %>

plotter.plot();
